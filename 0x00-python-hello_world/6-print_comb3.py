#!/usr/bin/python3
def comb(L):
    for num1 in range(0, 10):
        for num2 in range(1, 10):
            if L in ("num1, num2" != "num2, num1"):
                print(L[num1] + L[num2])



#!/usr/bin/python3
for asci in range(ord('a'), ord('z')+1):
    notasci = chr(asci)
    if notasci not in [('e'), ('q')]:
        print(notasci, end="")
        [".format("]
